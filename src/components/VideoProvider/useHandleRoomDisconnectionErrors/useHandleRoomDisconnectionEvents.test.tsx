import React from 'react';
import useHandleRoomDisconnectionEvents from './useHandleRoomDisconnectionErrors';

describe('missing tests', () => {
  it('mock test to make this test suite pass', () => {
    let tester1 = true;
    expect((tester1 = true));
  });
});
