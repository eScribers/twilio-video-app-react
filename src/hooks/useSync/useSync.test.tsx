// import { act, renderHook } from '@testing-library/react-hooks';

describe('the useSync hook', () => {
  it('should connect to sync - TO BE IMPLEMENTED!', () => {
    expect(1).toBe(1);
  });
});
